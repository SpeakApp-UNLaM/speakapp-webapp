import 'package:flutter/material.dart';

class SessionReports extends StatelessWidget {
  const SessionReports({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text("Informes de sesiones");
  }
}
